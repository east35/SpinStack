# Tailscale Configuration Example
# Copy this to .env.tailscale and update with your values
# Use with: docker-compose -f tailscaleproxy.yml --env-file .env.tailscale up -d

# Discogs API Credentials
DISCOGS_CONSUMER_KEY=your_consumer_key_here
DISCOGS_CONSUMER_SECRET=your_consumer_secret_here

# Session Secret (generate with: openssl rand -base64 32)
SESSION_SECRET=your_session_secret_here

# Tailscale URLs
# Replace 'spinstack' with your actual Tailscale service name
# The TSDProxy will create URLs like: https://spinstack.your-tailnet.ts.net
# IMPORTANT: Both URLs should point to the same Tailscale endpoint
# The nginx reverse proxy handles routing /api to backend internally
FRONTEND_URL=https://spinstack.your-tailnet.ts.net
NEXT_PUBLIC_API_URL=https://spinstack.your-tailnet.ts.net/api

# Setup Instructions:
# 1. Copy this file: cp .env.tailscale.example .env.tailscale
# 2. Copy nginx config: cp nginx-tailscale.conf.example nginx-tailscale.conf
# 3. Update .env.tailscale with your actual values
# 4. Update Discogs OAuth callback to: https://spinstack.your-tailnet.ts.net/auth/callback
# 5. Start with: docker-compose -f tailscaleproxy.yml --env-file .env.tailscale up -d
